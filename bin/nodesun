#! /usr/bin/env node

const NodeSun = require('../build/nodesun');
const packageJson = require('../package.json');

const nodesun = new NodeSun({
  version: packageJson.version,
  debug: false,
});

nodesun.run();
